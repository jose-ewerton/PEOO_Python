#Classe Pedido: Remover,Listar, Obter_valor_total
from item_pedido import ItemPedido
from produto import Produto

#ADICIONAR_PRODUTO
class Pedido:
    def adicionar_produto(self, quantidade): #ADICIONA_PRODUTO
        self.listar_produto.append(ItemPedido(quandidade))

  #REMOVER_PRODUTO
    def remover_item(self):
        remover =(input("Deseja remover algun item? Difite SIM ou NÂO"))
        if remover.upper() == "SIM":  
          self.listar_produto.remove(ItemPedido(quandidade))
        else:

  #VALOR_TOTAL
          def Obter_valor_total(self,valor_total,calculo):
            self.valor_total= None
            self.calculo= quandidade * self.__valor          




  