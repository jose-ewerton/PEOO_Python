"""
Faça um programa, cum uma função que necessite de um número variável de argumentos não 
nomeados e forneça a soma e quantidade de argumentos informados.
"""


def Numero(*args):
    lista = []
    for valores in args:
    	lista.append(valores)
    	print(f"{valores} inserido")
    	print("quantidade de argumentos: ", len(lista))
    	print("valor da soma dos argumentos: ", sum(lista))


Numero(18, 18, 19, 20)
